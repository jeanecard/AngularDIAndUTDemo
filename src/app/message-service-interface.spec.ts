import { MessageServiceInterface } from './message-service-interface';

describe('MessageServiceInterface', () => {
  it('should not create an instance', () => {
    //No way, can not instanciate interface.
    expect(false).toBeFalsy();
  });

});
