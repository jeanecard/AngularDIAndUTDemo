import { HttpClientHeroesGetter } from './http-client-heroes-getter';

describe('HttpClientHeroesGetter', () => {
  it('should create an instance', () => {
    expect("Here must be coded the previous heroes service glue to httpClient").toBeTruthy();
  });
});
